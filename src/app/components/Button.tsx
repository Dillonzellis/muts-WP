import React from 'react';

const Button = () => {
  return (
    <div className='inline-flex gap-2 border p-4'>
      <div>Apply Today</div>
      <div>Right Arr</div>
    </div>
  );
};

export default Button;
